# `c3-wip-fix` 변경 내용 안내

> 이 문서는 `c3-wip`에서 개인적으로 수정한 내용을 정리한 기록입니다. 원본 프로젝트나 원제작자의 공식 배포 안내가 아니며, 실제 차량 적용 결과는 차량 사양과 설정에 따라 달라질 수 있습니다.

기준 원본: `c3-wip` (`f2118063`)

문서 작성 기준: 2026-08-22

## 상태 표시

| 표시 | 의미 |
|---|---|
| ✅ | 코드 반영 후 실차에서 동작 확인 |
| 🟡 | 코드 반영됨, 추가 빌드 또는 실차 확인 필요 |
| ⚪ | 기존 기능 유지 |
| ⛔ | 의도적으로 비활성화 |

## 목차

1. [아이오닉 5 연결 및 크루즈](#1-아이오닉-5-연결-및-크루즈)
2. [SCC 차간거리](#2-scc-차간거리)
3. [시간 및 부팅](#3-시간-및-부팅)
4. [영상·로그·네트워크](#4-영상로그네트워크)
5. [설정 화면](#5-설정-화면)
6. [한국 오프라인 MAP](#6-한국-오프라인-map)
7. [주행 화면 UI](#7-주행-화면-ui)
8. [주요 설정값](#8-주요-설정값)
9. [검증 및 추가 확인 항목](#9-검증-및-추가-확인-항목)
10. [커밋 이력](#10-커밋-이력)

## 1. 아이오닉 5 연결 및 크루즈

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| ✅ | Panda 및 차량 연결 | 부팅 중 캐시 생성 작업이 Panda 시작을 방해하지 않도록 분리했으며, 아이오닉 5에서 `pandad`, `card`, `selfdrived` 실행을 확인했습니다. |
| ✅ | 순정 크루즈 작동 | Hyundai PCM 크루즈 경로에서 순정 크루즈 ON과 주행이 가능하도록 입력 처리를 보완했습니다. |
| ✅ | CANCEL 1회 해제 | 물리 CANCEL 입력 1회로 `buttonCancel`이 발생하며 즉시 해제됩니다. CANCEL 직후 PCM rising edge에 의해 다시 켜지는 동작도 차단했습니다. |
| ✅ | RESUME 작동 | CANCEL 또는 브레이크 해제 후 RESUME 버튼 입력이 동작하는 것을 실차에서 확인했습니다. |
| 🟡 | 저장속도 보존 | 브레이크 후 낮아진 현재 속도가 기존 설정속도를 덮어쓰지 않도록 수정하고 단위 테스트를 추가했습니다. 수정 후 다양한 속도 조건의 실차 재확인이 필요합니다. |
| 🟡 | 관련 설정 소비자 | `CruiseButtonMode`, `CancelButtonMode`, `AutoGasSyncSpeed` 경로가 순정 크루즈 동작과 충돌하지 않도록 분리했습니다. 설정 조합별 추가 실차 확인이 필요합니다. |

## 2. SCC 차간거리

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| 🟡 | CAN-FD 입력 | `SCC_CONTROL.DISTANCE_SETTING`이 수신되면 `carState.pcmCruiseGap`에 1~4 값을 유지하도록 수정했습니다. |
| 🟡 | UI 표시 | 성격 설정인 `LongitudinalPersonality + 1` 대신 실제 `pcmCruiseGap`을 사용하도록 변경했습니다. |
| 🟡 | 신호 부재 처리 | SCC 신호가 없는 프레임에서 임의 값으로 덮어쓰지 않고 마지막 유효값을 보존합니다. OP Long 구성별 실차 확인이 필요합니다. |

## 3. 시간 및 부팅

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| 🟡 | GPS 시간 동기화 | `system/timed.py`에서 GPS 시간 설정을 막던 경로를 수정했습니다. 시스템 시계는 UTC 기준을 유지합니다. |
| ✅ | 한국 시간 표시 | 주행 화면 날짜와 시간은 시스템 UTC와 분리하여 `Asia/Seoul` 기준으로 표시합니다. |
| 🟡 | SupportedCars 생성 | 제조사별 `values.py`를 부팅 critical path에서 연속 실행하지 않고 `startup_cache` 일회성 프로세스로 이동했습니다. |
| ✅ | Panda 시작 순서 | 캐시 생성 전에 Panda/UI 등 주요 프로세스가 먼저 시작되도록 변경해 차량 연결을 복구했습니다. |

## 4. 영상·로그·네트워크

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| ⛔ | 주행 영상 저장 | `encoderd`와 `stream_encoderd`를 비활성화했습니다. |
| ⚪ | 카메라 주행 파이프라인 | `camerad`, `modeld`, 운전자 모니터링 등 주행에 필요한 카메라 처리는 유지합니다. |
| ⚪ | 로컬 진단 로그 | `loggerd`, `logmessaged`, `logcatd`, `deleter`를 유지합니다. |
| ⛔ | comma Connect | 오프라인 사용 기준으로 `manage_athenad`를 비활성화했습니다. |
| ⛔ | 자동 업로드 | 표준 `uploader`를 실행 목록에서 제외했습니다. |
| ⛔ | Carrot heartbeat | `cweb_push` reporter/heartbeat 프로세스를 비활성화했습니다. |
| 🟡 | 사용자 데이터 공유 | `xiaoge_data`는 `ShareData=1`인 경우에만 실행합니다. 완전 오프라인 사용 시 `ShareData=0`을 사용해야 합니다. |

## 5. 설정 화면

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| ✅ | 메뉴 자동복귀 | CarrotPilot 설정 화면의 무입력 자동복귀 시간을 10초에서 20초로 변경했습니다. 화면 절전 시간은 변경하지 않았습니다. |
| 🟡 | 6개 하위 메뉴 설명 | Start, Cruise, Speed, Lat&Long, Display, Path 항목의 parameter 소비자를 확인해 한국어 설명을 보완했습니다. 항목명과 값 범위는 유지했습니다. |
| ✅ | 소프트웨어 브랜치 | 대상 브랜치 목록 조회와 선택, 업데이트/빌드/갱신 경로를 수정했습니다. |
| ✅ | 네트워크 설정 | 숨겨진 네트워크 사용 여부를 직접 선택할 수 있게 하고 SSH 항목과 구분했습니다. |
| ✅ | MAP 설정 | `MAP 데이터 사용`과 `한국 MAP 데이터 다운로드/갱신` 항목을 추가했습니다. |
| 🟡 | 화면 밝기 | Display 메뉴에 Sunny 방식의 자동/고정 밝기, 주행 중 어둡게 타이머, 어두운 밝기, 알림 시 화면 깨우기 설정을 추가했습니다. |

### 화면 밝기 설정

CarrotPilot의 `Display` 메뉴에서 설정합니다.

| 항목 | 값과 동작 |
|---|---|
| 화면 밝기 | `자동`은 전방 카메라 노출값으로 주변 밝기를 계산합니다. `5~100%`는 화면 밝기를 고정합니다. |
| 주행 화면 어둡게 타이머 | `항상 켜짐`(기본값), `15초`, `30초`, `1~10분` 중 선택합니다. |
| 어두워진 화면 밝기 | 타이머 종료 후 자동 밝기에 적용할 비율입니다. `0`은 백라이트를 어둡게 끕니다. |
| 일반 알림 시 화면 깨우기 | `사용`은 모든 알림, `미사용`은 사용자 확인·긴급 알림에서만 화면을 원래 밝기로 복원합니다. |

화면 터치는 타이머를 다시 시작합니다. 이 기능은 LCD 백라이트만 조절하며 카메라, 모델, CAN 및 주행 제어에는 영향을 주지 않습니다.

## 6. 한국 오프라인 MAP

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| ✅ | 도로명 표시 | GPS 위치와 한국 OSM 데이터를 사용한 현재 도로명 표시를 실차 화면에서 확인했습니다. |
| 🟡 | 한국 데이터 다운로드 | 다운로드 버튼을 누르면 `KR / South Korea` 데이터만 요청합니다. 전체 다운로드 완료와 갱신 동작은 네트워크 환경별 확인이 필요합니다. |
| ✅ | 오프라인 정책 | 데이터가 내려받아진 후 인터넷 없이 로컬 `mapd`에서 도로명을 제공합니다. |
| ✅ | MAP 미사용 | `MapEnable=0`이면 `mapd`, `mapd_manager`, 다운로드 요청 및 도로명 UI가 모두 중지됩니다. |
| ✅ | 재다운로드 방지 | 매 부팅마다 다운로드하지 않으며, 사용자가 다운로드/갱신 버튼을 누른 경우에만 새로 요청합니다. |

### MAP 사용 방법

1. 차량이 정차한 상태에서 CarrotPilot 설정의 Start 메뉴를 엽니다.
2. `MAP 데이터 사용` 값을 `1`로 설정합니다.
3. 바로 아래의 `한국 MAP 데이터`에서 `다운로드/갱신`을 누릅니다.
4. `다운로드 준비 중` → 진행률 → `다운로드 완료` 표시를 확인합니다.
5. 다운로드가 끝나면 인터넷 연결 없이 현재 도로명을 사용할 수 있습니다.

MAP을 사용하지 않을 때는 `MapEnable=0`으로 되돌리면 됩니다.

## 7. 주행 화면 UI

| 상태 | 항목 | 반영 내용 |
|---|---|---|
| ✅ | 도로명 | 현재 도로명을 화면 상단 중앙에 한 줄로 표시하며 긴 이름은 화면 폭에 맞게 줄입니다. |
| ✅ | STOP 카운터 | `carState.standstill`과 monotonic time을 사용해 정차 시간을 `STOP M:SS`로 표시하고 출발 시 즉시 초기화합니다. |
| 🟡 | 조향각 | TPMS 아래에 조향 아이콘과 실제 `steeringAngleDeg`를 표시합니다. `+`는 L, `-`는 R로 실차 부호를 맞췄으며 아이콘은 전체 조향각만큼 계속 회전합니다. 최신 아이콘 디자인은 추가 화면 확인이 필요합니다. |
| 🟡 | GPS 실제속도 | 계기판 속도와 별도로 GPS 속도를 날짜 아래에 `km/h`로 표시합니다. 최신 UI 빌드 후 실차 확인이 필요합니다. |
| 🟡 | GPS 과속 색상 | 제한속도 이하는 흰색, 초과 1~9km/h는 주황색, 초과 10km/h 이상은 빨간색입니다. 단속구간에서는 CAM 제한속도를 우선합니다. 이 색상은 경고 기준이며 실제 단속 개시 기준을 뜻하지 않습니다. |

### 화면 배치

- 좌상단: 시간 → 날짜/요일 → GPS 실제속도
- 상단 중앙: 현재 도로명 → STOP 카운터
- 우상단: TPMS
- TPMS 아래: 조향 아이콘 → 조향각 숫자와 L/R

## 8. 주요 설정값

### `Auto update Cruise speed` (`AutoGasSyncSpeed`)

| 값 | 동작 |
|---|---|
| `0` | 가속페달을 밟아도 저장된 크루즈 설정속도를 차량 현재속도로 자동 변경하지 않습니다. 순정형 CANCEL/RESUME 사용 시 권장합니다. |
| `1` | 크루즈 활성 상태에서 가속페달을 계속 밟으면 저장된 크루즈 속도가 차량속도까지 올라갈 수 있습니다. |

### `MAP 데이터 사용` (`MapEnable`)

| 값 | 동작 |
|---|---|
| `0` | MAP 프로세스, 다운로드 및 도로명 표시를 사용하지 않습니다. 기본값입니다. |
| `1` | 한국 오프라인 OSM과 도로명 표시를 사용합니다. 데이터 다운로드는 별도 버튼으로 시작합니다. |

### `Share Data` (`ShareData`)

| 값 | 동작 |
|---|---|
| `0` | 외부 TCP JSON 데이터 공유 프로세스를 실행하지 않습니다. 오프라인 사용 시 권장합니다. |
| `1` | `xiaoge_data` 데이터 공유 프로세스를 실행합니다. |

## 9. 검증 및 추가 확인 항목

### 확인된 내용

- 아이오닉 5에서 Panda와 차량 인식 복구
- 순정 크루즈 ON, CANCEL 1회 해제 및 RESUME 입력
- 한국 OSM 도로명과 STOP 카운터 표시
- 좌회전 `+ / L`, 우회전 `- / R` 조향 부호
- 소프트웨어 업데이트를 통한 `c3-wip-fix` 설치
- 변경 파일 diff 및 공백 오류 검사

### 추가 확인 필요

- 브레이크 감속 후 RESUME 시 기존 설정속도가 모든 속도 조건에서 유지되는지 확인
- OP Long ON/OFF 구성별 SCC GAP 1~4 신호와 UI의 1:1 일치 확인
- 최신 GPS 속도 글자 크기 및 100/101/109/110km/h 색상 경계 확인
- 조향 아이콘이 90도에서 멈추지 않고 실제 각도 전체 범위로 회전하는지 확인
- 한국 OSM 최초 다운로드, 갱신, 재부팅 후 오프라인 도로명 확인
- GPS UTC 시스템 시간과 `Asia/Seoul` 화면 시간 장시간 확인

> 최신 UI 변경은 로컬 Windows 환경에 SCons가 없어 전체 장비 빌드를 실행하지 못했습니다. comma 장비 업데이트 빌드와 정차 상태 화면 확인 후 상태를 갱신해야 합니다.

## 10. 커밋 이력

| 커밋 | 내용 |
|---|---|
| `b202233` | 오프라인 크루즈, UI 및 한국 MAP 기본 기능 추가 |
| `ae0f3bc` | CAN-FD, 부팅 및 오프라인 실행 경로 보강 |
| `1469edb` | 비활성 네트워크 reporter 잔여 코드 정리 |
| `71e0195` | MAP 다운로드와 설정 화면 흐름 수정 |
| `2f09c2d` | 소프트웨어 브랜치 선택과 업데이트 흐름 수정 |
| `e373b61` | Hyundai CAN 시작과 조향각 배치 수정 |
| `691a47a` | Panda 시작을 방해하던 캐시 프로세스 분리 |
| `64f0e40` | Hyundai PCM 크루즈와 조향 UI 수정 |
| `1a25800` | 도로명과 조향각 UI 배치 보완 |
| `0ffe6e3` | 조향 UI C++ lambda 빌드 오류 수정 |
| `6ec4991` | Hyundai PCM 크루즈 응답과 조향 UI 보완 |
| `675e744` | 아이오닉 5 순정 크루즈 인게이지 수정 |
| `61e0d09` | 도로명과 STOP 카운터 크기·간격 조정 |
| `a835a35` | 브레이크 후 RESUME 저장속도 보존 |
| `09e8313` | GPS 실제속도·과속 색상 및 조향 UI 개선 |
